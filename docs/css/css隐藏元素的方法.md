# css隐藏元素

## 实现
```css
 /* 隐藏且不渲染该元素 */
display: none;

 /* 隐藏且占位 */
visibility: hidden;

 /* 隐藏且占位 */
opacity: 0;

/* 标签宽高为0*0 */
transform: scale(0);

/* 占位且box多了margin值 */
margin-left: -100%;

```
